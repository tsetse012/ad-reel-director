# Stills (concepts / sheets / keyframes) — OpenAI Images API, model gpt-image-2
OPENAI_API_KEY=

# Video — Atlas Cloud, Seedance 2.5 (needs a key that is allowed to call video models;
# an Atlas "coding plan" key returns 403 "this model not support coding plan")
ATLASCLOUD_VIDEO_API_KEY=

# Optional — only for scripts/mix_bgm.sh if you generate BGM with ElevenLabs Music
ELEVENLABS_API_KEY=
